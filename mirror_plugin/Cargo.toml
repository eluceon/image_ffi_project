[package]
name = "mirror_plugin"
version = "0.1.0"
edition = "2021"
description = "Mirror/flip image plugin — reflects image horizontally and/or vertically"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
